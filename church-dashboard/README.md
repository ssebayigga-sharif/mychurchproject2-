# Church Dashboard

Next.js dashboard for church management workflows.

## Scripts

```bash
npm run dev
npm run build
npm run lint
```

The app uses the Next.js App Router under `app/`, shared UI under `components/`, and services/context/types under `lib/`.
